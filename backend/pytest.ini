[pytest]
pythonpath = .
testpaths =
    tests
    .
markers =
    integration: tests that require MongoDB (TestClient + zenith_test database)
    unit: fast isolated tests with mocks (default for legacy tests)
filterwarnings =
    ignore::DeprecationWarning
